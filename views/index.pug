doctype html
html
  head
    meta(name='viewport', content='width=device-width, initial-scale=1')
    title Lupussino online!
    link(rel='stylesheet', href='/stylesheets/style.css')
    link(rel='stylesheet', href='/stylesheets/all.min.css')

  body
    div#app

      //-FORM INIZIALE
      div#input-cnt(v-if="!(player.is_online)")
        div(class="label") Username
        input#player(v-model="player.name", type="text", required="true")
        div(class="label") Password
        input#password(v-model="player.password", type="password", required="true")
        div(class="label") Stanza
        input#room(v-model="room.name", type="text", required="true", list="room-list")

        datalist#room-list
          room(v-for="room in room_list" v-bind:name="room.name" v-bind:players="room.player_list.filter(x => x.is_online).length" v-bind:key="room.id")

        div#enter(@click="room_enter", class="btn") Entra!

        span(class="error", v-if="errors.player_name_already_in_use") Nickname già utilizzato!
        span(class="error", v-if="errors.missing_login_data") Inserisci un nickname e una stanza!
        span(class="error", v-if="errors.wrong_password") Password errata!


      //-GIOCO
      div#role-cnt(v-if="player.is_online")

        div#player-list-cnt(v-bind:class="[(player_list_open) ? 'open':'close']")
          div#player-list-title
            span#setting-toggle(class="fas fa-cog", v-show="player.is_owner", @click="open_settings", v-bind:class="[(setting_window_open) ? 'open':'close']")
            span(v-bind:class="[(setting_window_open) ? 'blur':'']") Stanza {{room.name}} - {{player_number}}/{{room.player_list.length}}
            span#player-list-toggle(class="fas fa-arrow-alt-circle-up", @click="player_list_open = !player_list_open", v-bind:class="[(setting_window_open) ? 'blur':'']")
          div#player-list(v-bind:class="[(card_face_up) ? 'up':'down', (setting_window_open) ? 'blur':'']")
            player(v-bind:name="player.name" v-bind:score="player.score" v-bind:status="player.is_online" v-bind:owner="player.is_owner" v-bind:key="player.id" v-bind:crown="player.is_owner" v-bind:myself="true")
            player(v-for="p in room.player_list" v-if="p.name!=player.name" v-bind:name="p.name" v-bind:score="p.score" v-bind:status="p.is_online" v-bind:owner="player.is_owner" v-bind:key="p.id" v-bind:crown="p.is_owner" v-bind:myself="player.id==p.id")

        div(class="flip-card" @click="card_face_up = !card_face_up" v-bind:class="[(card_face_up) ? 'up':'down', (setting_window_open) ? 'blur':'']")
          div(class="flip-card-inner")
            div(class="flip-card-front")
              transition(name="slide-fade", mode="out-in")
                div#role-description-cnt(:key="role_animation")
                  div#role {{ player.role.name }}
                  div#detail {{ player.role.detail }}
            div(class="flip-card-back")

        div#votazione(v-show="player_number > 2 && player_number < 7 && room.game_started && !room.vote_ended")
          select(v-model="player.player_voted")
            option(disabled selected value="") Votazione...
            option Cielo
            option(v-for="p in room.player_list.filter(x => x.name!=player.name && x.role.detail!='')" v-if="p.name!=player.name") {{p.name}}
          div#vote(@click="vote", class="btn" v-bind:class="(player.has_voted) ? 'annulla':''") {{vote_text}}
          div#vote-detail Hanno votato {{room.player_list.filter(x => x.has_voted).length}}/{{room.player_list.filter(x => x.role.detail!='').length}}

        div#new-game(@click="game", v-show="player.is_owner && player_number > 2 && player_number < 7", class="btn") Nuova partita!
          


      div#setting-window(v-bind:class="[(setting_window_open) ? 'open':'close']")
        div#setting-list
          div(class="setting" v-show="player_number > 2 && player_number < 7") Ruoli attivi:
            ul
              li(class="role", v-for="r in room.configurazioni[player_number]") {{ r }}

          div(class="setting") Mitomane riconosce Assassini
            label(class="switch")
              input(type="checkbox", v-model="room.mitomane_riconosce_assassini")
              span(class="slider")

          div(class="setting") Testimoni si riconoscono
            label(class="switch")
              input(type="checkbox", v-model="room.testimoni_si_riconoscono")
              span(class="slider")

        div#save-setting(@click="update_settings",class="btn") Salva impostazioni

  script(src='https://cdnjs.cloudflare.com/ajax/libs/socket.io/2.2.0/socket.io.dev.js')
  script(src='https://cdn.jsdelivr.net/npm/vue/dist/vue.js')
  script(src='/javascripts/main.js')